///get_stem_index(gen, num)
var genk = argument0, numk = argument1, iik = 0;
for(ik = 0; ik < array_height_2d(stem); ik++){
    if stem[ik, 0] == genk{
        if iik = numk{
            return ik;
        }else{
            iik++;
        }
    }
}
